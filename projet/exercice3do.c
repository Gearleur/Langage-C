#include <stdio.h>
#include <math.h>

/* afficher le code ascci */

void main()
{
    char c;
    char co;
    printf("demander la lettre\n");
    scanf("%c", &c);
    printf("caractère = %c, code = %d, code hexa = %x\n", c, c, c);
    for (co == 'A'; co <= 'Z'; co++)
    {
        printf("caractère = %c, code = %d, code hexa = %x\n", co, co, co);
    }
    for (co == '1'; co<='9'; co++)
    {
        printf("caractère = %c, code = %d, code hexa = %x\n", co, co, co);
    }
}