#include <stdio.h>
#include <math.h>

/* produit de deux entier */

void main()
{
    int a;
    int b;
    printf("les valeur des deux facteur\n");
    scanf("%d %d", &a, &b);
    printf("le produit des valeur %d et %d est = %d", a, b, a*b);
}